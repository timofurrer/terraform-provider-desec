---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "desec_rrset Resource - desec"
subcategory: ""
description: |-
  Manages a deSEC DNS Resource Record Set (RRset). An RRset is the set of all DNS records of a given type for a given name within a domain.
  Use @ as the subname to manage records at the zone apex (root of the domain).
---

# desec_rrset (Resource)

Manages a deSEC DNS Resource Record Set (RRset). An RRset is the set of all DNS records of a given type for a given name within a domain.

Use `@` as the `subname` to manage records at the zone apex (root of the domain).

## Example Usage

```terraform
# Create a DNS zone first
resource "desec_domain" "example" {
  name = "example.com"
}

# Create an A RRset for www.example.com
resource "desec_rrset" "www_a" {
  domain  = desec_domain.example.name
  subname = "www"
  type    = "A"
  ttl     = 3600
  rdata   = ["1.2.3.4", "5.6.7.8"]
}

# Create an AAAA RRset for www.example.com
resource "desec_rrset" "www_aaaa" {
  domain  = desec_domain.example.name
  subname = "www"
  type    = "AAAA"
  ttl     = 3600
  rdata   = ["2001:db8::1"]
}

# Create an MX RRset at the zone apex
resource "desec_rrset" "mx" {
  domain  = desec_domain.example.name
  subname = "@"
  type    = "MX"
  ttl     = 3600
  rdata   = ["10 mail.example.com.", "20 mail2.example.com."]
}

# Create a TXT RRset for SPF
resource "desec_rrset" "spf" {
  domain  = desec_domain.example.name
  subname = "@"
  type    = "TXT"
  ttl     = 3600
  rdata   = ["\"v=spf1 include:_spf.example.com ~all\""]
}

# Create a CNAME RRset
resource "desec_rrset" "blog_cname" {
  domain  = desec_domain.example.name
  subname = "blog"
  type    = "CNAME"
  ttl     = 3600
  rdata   = ["www.example.com."]
}

# Publish an OpenPGP public key as a DANE OPENPGPKEY record (RFC 7929)
locals {
  dane = provider::desec::openpgpkey_dane("hugh@example.com", file("hugh.gpg.base64"))
}

resource "desec_record" "openpgpkey" {
  domain  = local.dane.domain
  subname = local.dane.subname
  type    = local.dane.type
  ttl     = 3600
  records = [local.dane.rdata]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `domain` (String) The domain name that this RRset belongs to.
- `rdata` (Set of String) The set of RDATA strings. The format depends on the record type. For example, `A` records contain IPv4 addresses, `MX` records contain `priority hostname.` pairs.
- `subname` (String) The subdomain part of the RRset name. Use `@` or `""` for the zone apex (root of the domain).
- `ttl` (Number) The TTL (time-to-live) in seconds. Must be at least the domain's `minimum_ttl`.
- `type` (String) The DNS record type (e.g. `A`, `AAAA`, `CNAME`, `MX`, `TXT`). Must be uppercase.

### Read-Only

- `created` (String) Timestamp of RRset creation in ISO 8601 format.
- `id` (String) The resource identifier in the form `domain/subname/type`.
- `touched` (String) Timestamp of when the RRset was last touched.

## Import

Import is supported using the following syntax:

In Terraform v1.12.0 and later, the [`import` block](https://developer.hashicorp.com/terraform/language/import) can be used with the `identity` attribute, for example:

```terraform
import {
  to = desec_rrset.www_a
  identity = {
    domain  = "example.com"
    subname = "www"
    type    = "A"
  }
}
```

<!-- schema generated by tfplugindocs -->
### Identity Schema

#### Required

- `domain` (String) The domain name this RRset belongs to.
- `subname` (String) The subdomain part of the RRset name.
- `type` (String) The DNS record type (e.g. A, AAAA, TXT).

In Terraform v1.5.0 and later, the [`import` block](https://developer.hashicorp.com/terraform/language/import) can be used with the `id` attribute, for example:

```terraform
import {
  to = desec_rrset.www_a
  id = "example.com/@/A"
}
```

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import desec_rrset.www_a example.com/@/A
```
