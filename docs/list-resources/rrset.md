---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "desec_rrset List Resource - desec"
subcategory: ""
description: |-
  Lists deSEC DNS resource record sets (RRsets) within a domain.
---

# desec_rrset (List Resource)

Lists deSEC DNS resource record sets (RRsets) within a domain.

## Example Usage

```terraform
# List all RRsets within a domain
list "desec_rrset" "all" {
  provider = desec
  config {
    domain = "example.com"
  }
}

# List only A RRsets within a domain
list "desec_rrset" "a_records" {
  provider = desec
  config {
    domain = "example.com"
    type   = "A"
  }
}

# List RRsets for a specific subdomain
list "desec_rrset" "www" {
  provider = desec
  config {
    domain  = "example.com"
    subname = "www"
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `domain` (String) The domain name to list RRsets for.

### Optional

- `subname` (String) Filter by subdomain part of the RRset name.
- `type` (String) Filter by DNS record type (e.g. `A`, `AAAA`, `TXT`).
